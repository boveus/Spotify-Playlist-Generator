<h3>Search Results</h3>
<% @search_results.each_with_index do |artist, index| %>
<tr>
  <% num = index + 1 %>
   <td class=top_artist id=top_artist_<%= num %>><%= num %> : <%= artist.name %></td>
   <br />
</tr>
<% end %>
